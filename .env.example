# Copy to .env and fill in real values. Never commit .env.

# Robot names (must match robot_name used in start.launch.py)
ROBOT1_NAME=Beaker
ROBOT2_NAME=YourRobot2Name

# Camera
CAMERA_IP=0.0.0.0

# Modbus server — runs on Robot 2's machine
MODBUS_HOST=0.0.0.0
MODBUS_PORT=5020
