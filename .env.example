# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials or IP addresses.

# FANUC robot IP address (e.g. 10.8.4.16)
ROBOT_IP=0.0.0.0

# Robot name — used to namespace ROS2 topics/actions
ROBOT_NAME=fanuc
