# Trigger the camera node to capture an image and count dice pips.
# No request fields needed -- just send the request to trigger capture.
---
int32 pip_count       # Number of pips detected (0 if detection failed)
bool success          # True if image was captured and processed successfully
string message        # Human-readable status or error description
