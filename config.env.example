# Copy this file to config.env and fill in your local values.
# config.env is gitignored — never commit it.

# GigE camera IP address
CAMERA_IP=<your-camera-ip>

# YOLO model run name (folder under runs/classify/)
MODEL_NAME=dice_class5
